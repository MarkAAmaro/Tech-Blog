{{#each blogs}}
    <div class="blog-post">
        <h2><a href="/blog/{{id}}">{{title}}</a></h2>
        <p>{{content}}</p>
        <p>Posted by {{User.username}} on {{date_created}}</p>
    </div>
{{/each}}
